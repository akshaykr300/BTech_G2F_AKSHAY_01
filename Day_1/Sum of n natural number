#include <stdio.h>

int main() {
    int n, i, sum = 0;

    printf("Enter a number: ");
    scanf("%d", &n);
     printf("The n is = %d\n",n);


    for(i = 1; i <= n; i++) {
        sum = sum + i;
    }
\\Sum of n natural no
    printf("Sum of first %d natural numbers = %d", n, sum);

    return 0;
}
